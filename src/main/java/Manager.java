public class Manager extends Employee {

    // Instance Constants
    final private double DISCOUNT = 0.15;

    // Constructors
    public Manager (String name, Product[] products, Size size) {
        super(name, products, size);
    }

}